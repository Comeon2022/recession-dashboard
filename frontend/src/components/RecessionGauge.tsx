import type {DashboardData} from '../types/dashboard'; export function RecessionGauge({data}:{data:DashboardData}){const rotation=-90+(data.total_score/data.max_score)*180;return <div className="gauge-wrap"><div className="gauge" style={{'--needle':`${rotation}deg`} as React.CSSProperties}><div className="gauge-needle"/></div><div className="gauge-labels"><span>Healthy</span><span>Slowdown</span><span>Recession</span></div><div className="score"><strong>{data.total_score}</strong><span> / {data.max_score}</span></div><div className="regime">{data.regime}</div></div>}
