import type {Indicator} from '../types/dashboard'; import {IndicatorCard} from './IndicatorCard'; import {TreasuryCurvePanel} from './TreasuryCurvePanel'; type CurveCarrier=Indicator & {treasury_curve?:Parameters<typeof TreasuryCurvePanel>[0]['curve']}; export function IndicatorGrid({indicators}:{indicators:Indicator[]}){const curve=indicators.find((indicator)=>indicator.id==='yield-curve') as CurveCarrier|undefined; return <section className="indicator-grid">{indicators.map(i=><IndicatorCard key={i.id} indicator={i}/>)}{curve?.treasury_curve ? <TreasuryCurvePanel curve={curve.treasury_curve} /> : null}</section>}
